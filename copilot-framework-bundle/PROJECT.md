
# PROJECT.md

<!--
  What this project is and where it stands. This is the project's own file, kept
  separate from any one assistant's instructions so every assistant reads the
  same state - Claude Code loads it via an import in `CLAUDE.md`, GitHub Copilot
  through its own instructions file.

  Committed, so it is shared with everyone on the project and holds project facts
  only. Personal settings - which language to speak, who commits - live in the
  gitignored `PROJECT.local.md`, one copy per person.

  Always written in English, whatever language the assistant speaks to the user
  in, so anyone who joins the project can read it.

  Keep it under about 200 lines. Status is two fields; the rest is the overview.
  Maintained by /start and /end, never after individual coding tasks.
-->

## Project status

<!-- The single source of truth for whether the project is initialized. Do not
     add personal settings here - they belong in `PROJECT.local.md`. -->

- **Initialized:** no
- **Repository:** _[link]_

## Overview

<!-- Required. In plain language a human can follow: what is this project, what
     does it do, and for whom? Filled on first run, kept current at /end. -->
