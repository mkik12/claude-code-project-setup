# _[application name]_

**Owner:** _[the developer who started the project]_

## Overview

_[What the app does, in a few sentences. Keep it short - each app has its own
external documentation for the detail.]_

## Running the app

_[How to run it locally: create the `.venv`, install `requirements.txt`, start
the app (`python src/app.py`), and open the local URL (e.g.
http://127.0.0.1:8080).]_

## External resources

_[The external APIs and endpoints the app needs to work. Write "None" if the app
is self-contained.]_
