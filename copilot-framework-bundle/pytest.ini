[pytest]
# Puts the project root on the import path so tests use `from src.app import ...`
# without a PYTHONPATH prefix on every command. Set here once, per flask.md.
pythonpath = .
testpaths = tests
