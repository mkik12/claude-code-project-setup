---
description: Publish the app to CodeNow (stub - not implemented yet).
---

This command is a stub. Tell the user that publishing is not implemented yet and
that, for now, they publish manually in CodeNow. Do not attempt to publish.

Planned behavior: pre-flight checks (everything committed and pushed, tests
green, pylint clean, the app runs locally and `/health` responds), then a
walkthrough of CodeNow's manual publish steps, then verify the live deployment
responds.
